mongodb:
  database: graylog
  rootPassword: "{{ lookup('password', '/dev/null length=60 chars=ascii_letters') }}"
  password: "{{ lookup('password', '/dev/null length=60 chars=ascii_letters') }}"
  username: graylog
  replica_set_key: "{{ lookup('password', '/dev/null length=60 chars=ascii_letters') }}"
