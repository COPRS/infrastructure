apiVersion: stash.appscode.com/v1alpha1
kind: Repository
metadata:
  name: s3-postgresql-backup
spec:
  backend:
    s3:
      endpoint: {{ stash.postgresql.endpoint }}
      bucket: {{ stash.postgresql.name }}
      region: {{ stash.postgresql.region }}
    storageSecretName: stash-s3-credentials
