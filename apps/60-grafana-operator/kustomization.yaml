namespace: infra
namePrefix: {{ app_name }}-

commonLabels:
  app.kubernetes.io/instance: {{ app_name }}

resources:
  - deployment.yaml
  - rbac.yaml
  - servicemonitor.yaml
  - service.yaml

configMapGenerator:
- name: manager-config
  files:
    - controller_manager_config.yaml
