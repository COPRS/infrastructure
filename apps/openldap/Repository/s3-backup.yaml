apiVersion: stash.appscode.com/v1alpha1
kind: Repository
metadata:
  name: s3-ldap-backup
spec:
  backend:
    s3:
      endpoint: {{ stash.openldap.endpoint }}
      bucket: {{ stash.openldap.name }}
      region: {{ stash.openldap.region }}
    storageSecretName: stash-s3-credentials
