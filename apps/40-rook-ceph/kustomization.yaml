commonLabels:
  app.kubernetes.io/instance: {{ app_name }}

namespace: infra

helmCharts:
- name: {{ app_name }}
  repo: https://charts.rook.io/release
  version: v1.7.7
  valuesFile: values.yaml
  namespace: infra
