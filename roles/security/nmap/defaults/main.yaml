---
nmap:

  # Requiered
  nmap_version: "7.80+dfsg1-2build1"
  
  cron_scan:
    name: "# NmapScan PORT #"
    hour: "6"
  
  # Requires if load balancer is used (this value is use on master nmap)
  #domain_balancer_ip: XX.XX.XX.XX