kind: Secret
apiVersion: v1
metadata:
  name: elasticsearch-trace-filerealm-secret
stringData:
  users: |-
    operator:{{ elasticsearch_trace.operator_password_hash }}
  users_roles: |-
    monitoring_user:operator
    reporting_user:operator
    kibana_admin:operator
