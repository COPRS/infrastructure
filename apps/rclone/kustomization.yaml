namespace: security

commonLabels:
  app.kubernetes.io/instance: "{{ app_name }}"

resources:
  - clone-elasticsearch-processing-backup.yaml
  - clone-postgresql-backup.yaml
  - clone-ldap-backup.yaml

configMapGenerator:
  - name: rclone-entrypoint
    files:
      - ./entrypoint.sh

secretGenerator:
  - name: rclone-conf
    files:
      - rclone.conf
