global:
  storageClass: ceph-block
server:
  image:
    tag: 2.9.1-debian-10-r7
  composedTaskRunner:
    image:
      tag: 2.9.1-debian-10-r7
  # no node match label node-role.kubernetes.io/worker
  # affinity:
  #   nodeAffinity:
  #     requiredDuringSchedulingIgnoredDuringExecution:
  #       nodeSelectorTerms:
  #       - matchExpressions:
  #         - key: "node-role.kubernetes.io/worker"
  #           operator: In
  #           values:
  #           - processing
skipper:
  image:
    tag: 2.8.1-debian-10-r6
  # no node match label node-role.kubernetes.io/worker
  # affinity:
  #   nodeAffinity:
  #     requiredDuringSchedulingIgnoredDuringExecution:
  #       nodeSelectorTerms:
  #       - matchExpressions:
  #         - key: "node-role.kubernetes.io/worker"
  #           operator: In
  #           values:
  #           - processing
waitForBackends:
  image:
    tag: 1.22.2-debian-10-r39
mariadb:
  enabled: false
rabbitmq:
  enabled: false
externalDatabase:
  host: postgresql.infra.svc.cluster.local
  port: 5432
  driver: org.postgresql.Driver
  scheme: postgresql
  password: p0stgrespassword
  dataflow:
    database: infra
    username: postgres
  skipper:
    database: infra
    username: postgres
externalKafka:
  enabled: true
  brokers: kafka.infra.svc.cluster.local:9092
  zkNodes: kafka-zookeeper.infra.svc.cluster.local:2181
