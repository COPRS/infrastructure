apiVersion: stash.appscode.com/v1alpha1
kind: Repository
metadata:
  name: s3-ldap-backup
spec:
  backend:
    s3:
      endpoint: {{ openldap.backup.s3.endpoint }}
      bucket: {{ openldap.backup.s3.bucket }}
      region: {{ openldap.backup.s3.region }}
    storageSecretName: openldap-backup-s3-credentials
