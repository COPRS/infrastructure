commonLabels:
  app.kubernetes.io/instance: "{{ app_name }}"

resources:
  - producer.yaml
  - consumer.yaml
  - topic.yaml
  - scaledobject.yaml

configMapGenerator:
  - name: producer
    files:
      - producer.py
  - name: consumer
    files:
      - consumer.py
