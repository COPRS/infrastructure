commonLabels:
  app.kubernetes.io/instance: {{ app_name }}

helmChartInflationGenerator:
- chartName: {{ app_name }}
  chartRepoUrl: https://charts.rook.io/release
  chartVersion: v1.7.7
  releaseNamespace: infra
  values: values.yaml
